#!/usr/bin/env bash
# Todo link de um post.md numa tabela. O código está em _lib/listar-links.py;
# aqui só se acha o Python do sistema (ver _lib/python).
#
# Uso: scripts/listar-links post.md > links.md
exec bash "$(dirname "$0")/_lib/python" "$(dirname "$0")/_lib/listar-links.py" "$@"
